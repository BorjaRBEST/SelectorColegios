name: selectorcolegios
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  http: ^1.2.1
  excel: ^4.0.3
  path_provider: ^2.1.3
  reorderables: ^0.6.0
  pdf: ^3.10.8
  flutter_map: ^7.0.1
  latlong2: ^0.9.1
  google_maps_flutter: ^2.2.8
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/colegios.json

